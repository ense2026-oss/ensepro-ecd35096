# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
# NOTE: .env is intentionally tracked — the deploy pipeline builds directly
# from this repo and needs VITE_SUPABASE_* baked in at build time. It only
# contains the Supabase anon/publishable key, which is safe to expose.
.env.local
.env.*.local
