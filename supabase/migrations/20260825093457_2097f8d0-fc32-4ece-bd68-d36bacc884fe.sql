CREATE POLICY "Users can update own leave attachments" ON storage.objects FOR UPDATE TO authenticated USING (bucket_id = 'leave-attachments' AND (auth.uid())::text = (storage.foldername(name))[1]) WITH CHECK (bucket_id = 'leave-attachments' AND (auth.uid())::text = (storage.foldername(name))[1]);